# OSD 滚动字幕模块 — 功能需求文档

## 一、模块定位

OSD 滚动字幕模块用于对选中的屏组循环设置不同的字幕移动效果。设备支持多个屏幕（屏组），每个屏幕有独立的字幕列表（MoveID），每条字幕包含方向、速度、帧数等参数。模块按顺序依次将每条字幕设置到每个屏幕上，每屏独立维护自己的字幕索引。

---

## 二、前置条件

- 用户已成功登录设备
- 登录后自动加载屏组列表
- 至少选中一个屏组
- 字幕列表已通过「查询字幕列表」按钮加载（在 OSD Tab 中）

---

## 三、参数说明

| 参数 | 控件类型 | 默认值 | 说明 |
|------|---------|--------|------|
| 停留时间 | 数字输入框 (ms) | — | 每条字幕显示的时间，最小 500ms |

**注**：OSD 模块不依赖信号源输入框，无信号源相关参数。

---

## 四、前置操作：查询字幕列表

1. 用户点击「查询字幕列表」按钮
2. **依次向每个选中的屏组**发送**查询 OSD 字幕列表**请求（协议 80874）
3. 解析每个响应：
   - 提取该屏组的所有 MoveID 条目（每条含 `id`、`name`、`direction`、`mov_step`、`frame_count`）
   - 提取该屏组的分辨率（`res_w`、`res_h`）
4. 将各屏组的字幕列表存入 `_osd_screen_moves[screen_id]`，分辨率存入 `_osd_res[(screen_id,)]`
5. 将字幕展示在表格中（3 列：屏组 ID、MoveID、字幕名称）
6. 状态标签更新：
   - 有字幕：`共 N 个字幕，可开始轮播`（绿色）
   - 无字幕：`未查询到字幕`（红色）

---

## 五、验证规则（点击开始时执行）

1. **屏组检查**：未选中任何屏组 → 弹出警告框"请先选择至少一个屏组"
2. **字幕列表检查**：`_osd_screen_moves` 为空 → 弹出警告框"请先点击「查询字幕列表」"
3. **有效屏组过滤**：所有选中屏组在 `_osd_screen_moves` 中均无字幕 → 弹出警告框"所有选中屏组均无字幕，请重新查询"
4. **停留时间检查**：停留时间小于 500ms → 弹出警告框"停留时间最小为 500ms"

---

## 六、开始流程

1. 检查步进/随机/底图模块是否运行，如有则先停止
2. 执行上述验证规则
3. **过滤掉无字幕的屏组**（保留有字幕的屏组）
4. 禁用"开始"按钮和"步进/随机/底图开始"按钮，启用"停止"按钮
5. 为每个有效屏组创建独立的字幕索引 `move_indices[screen_id] = 0`
6. 创建状态字典 `_osd_state`，包含 groups、move_indices、dwell_ms、_screen_idx
7. 立即触发第一次轮播 `_do_osd_cycle()`

---

## 七、循环逻辑

与底图轮询类似，OSD 也是**逐个屏幕串行处理**，每完成一个屏幕后启动定时器等待 dwell，再处理下一个。

### 字幕随机参数

模块在设置每条字幕时，从设备查询到的参数基础上**附加随机行为**：

| 参数 | 随机来源 | 说明 |
|------|---------|------|
| 方向 | `OSD_DIRECTIONS = [2,3,0,1,6]`（上、下、左、右、静止）随机选择 | 不使用设备原始方向 |
| 速度 | `OSD_SPEEDS = [4,2,1]`（快、较快、慢）随机选择 | 不使用设备原始速度 |
| 起始位置 | 在该屏幕分辨率范围内随机（start_row、start_col） | 确保字幕起始位置在屏幕内 |

### 主体流程（`_do_osd_cycle`）

1. 如果 `_osd_running = False` 则返回
2. 根据 `_screen_idx` 取当前要处理的屏组 `gid`
3. 根据该屏组的字幕索引 `move_indices[gid]` 取当前字幕 `mv`
4. 随机选择方向和速度，在屏幕分辨率范围内随机生成起始位置
5. 从字幕 `mv` 中取出 `frame_count`（查不到则用默认值 2047）
6. 构建设置请求（协议 80862，**仅更新指定字段，颜色字段留空，设备保留原有颜色**）
7. 启动 **WallWorker**（支持诊断日志）发送请求
8. worker 的 `finished` 信号触发 `_on_osd_one_done()`

### 单个屏幕完成后（`_on_osd_one_done`）

1. 清空 `_osd_worker`
2. 如果 `_osd_running = False` 则直接返回
3. 推进该屏组的 MoveID 索引：`move_indices[gid] = (old + 1) % len(moves)`
4. 推进屏组指针：`state["_screen_idx"] = (sidx + 1) % len(groups)`
5. **刚轮完一圈的判断**（`_screen_idx` 从最后一个屏组绕回 0）：
   - 将屏组指针重置为 0（注意：不重置 `move_indices`，各屏组的 MoveID 独立循环）
6. 启动**停留定时器**（`_osd_timer.start(dwell_ms)`）

### 防重入机制

- `_osd_running` 标志：作为循环入口的守卫条件

---

## 八、停止流程

1. 设置 `_osd_running = False`
2. 停止定时器（`_osd_timer.stop()`）
3. 如果 worker 还在运行：设置 `_worker._stop = True`，等待其结束后清空
4. 恢复 UI 状态：启用"开始"、"步进/随机/底图开始"按钮，禁用"停止"按钮
5. **不清空 `_osd_state` 和 `_osd_screen_moves`**（下次启动时直接复用）

---

## 九、UI 状态说明

| 状态 | 开始按钮 | 停止按钮 | 步进/随机/底图开始 | 字幕索引显示 |
|------|---------|---------|-----------------|------------|
| 未运行 | 启用 | 禁用 | 启用 | — |
| 运行中 | 禁用 | 启用 | 禁用 | `屏组{id}: {序号}/{总数} (MoveID={id})` |
| 停止中 | 启用 | 禁用 | 启用 | — |

---

## 十、多屏组独立索引示意

假设屏组 G1 有字幕 [A, B]，G2 有字幕 [X, Y, Z]：

```
G1 应用 A → G1 索引: B
G2 应用 X → G2 索引: Y
G1 应用 B → G1 索引: A（绕回）
G2 应用 Y → G2 索引: Z
G1 应用 A → G1 索引: B
G2 应用 Z → G2 索引: X（绕回）
→ G2 归零继续，G1 继续独立循环
```

---

## 十一、与底图轮询的差异对照

| 特性 | 底图轮询 | OSD 字幕 |
|------|---------|---------|
| 查询协议 | 81204 | 80874 |
| 设置协议 | 80850 | 80862 |
| 定时器启动时机 | 每屏完成后 | 每屏完成后 |
| 一轮完成的处理 | 重置所有屏组的索引 | 只重置屏组指针，不重置 MoveID 索引 |
| 方向/速度参数 | 设备返回（随机选择物理屏） | 随机生成（不看设备原值） |
| Worker 类型 | BkCycleWorker | WallWorker |

---

## 十二、协议号对照

| 功能 | 协议号 |
|------|--------|
| 查询 OSD 字幕列表 | 80874 |
| 设置 OSD 移动效果 | 80862 |

---

## 十三、模块互斥

- 开始前检查步进/随机/底图模块是否运行：如有，先停止
- 运行期间，步进/随机/底图开始按钮被禁用
