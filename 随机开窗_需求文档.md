# 随机开窗模块 — 功能需求文档

## 一、模块定位

随机开窗模块用于在选中的屏组上，每轮随机生成多个窗口（随机位置、随机尺寸），每次循环使用下一个信号源。与步进开窗不同，随机开窗的窗口位置和尺寸在每轮都是随机生成的。

---

## 二、前置条件

- 用户已成功登录设备
- 登录后自动加载信号源列表和屏组列表
- 至少选中一个屏组
- 已在信号源输入框中填写了有效的信号源范围

---

## 三、参数说明

| 参数 | 控件类型 | 默认值 | 说明 |
|------|---------|--------|------|
| 窗口数量 | 数字输入框 | — | 每轮每个屏组同时开的窗口数 |
| 宽上限 | 数字输入框 | — | 随机窗口的最大宽度，最小 100 |
| 高上限 | 数字输入框 | — | 随机窗口的最大高度，最小 100 |
| 停留时间 | 数字输入框 (ms) | — | 开窗后保持的时间 |
| 间隔时间 | 数字输入框 (ms) | — | 每次循环的总间隔 |
| 信号源范围 | 文本框 | — | 支持 `0-3` 或 `0,1,2,3` 格式 |

---

## 四、验证规则（点击开始时执行）

1. **屏组检查**：未选中任何屏组 → 弹出警告框"请先选择至少一个屏组"
2. **信号源检查**：信号源输入为空或格式错误 → 弹出警告框"请填写正确的信号源"
3. **尺寸上限检查**：宽上限或高上限小于 100 → 弹出警告框"窗口宽高最大值为 100"
4. **范围检查**：每个选中屏组的范围必须能容纳最小窗口 100x100，不满足则弹出警告框

范围默认值：宽 3840，高 2160。

---

## 五、开始流程

1. 检查步进开窗是否运行，如有则先停止
2. 执行上述验证规则，全部通过后继续
3. 禁用"开始"按钮和"步进开窗开始"按钮，启用"停止"按钮
4. 创建状态字典 `_rand_state`，保存所有参数
5. 用信号源列表创建无限迭代器 `itertools.cycle(src_ids)`
6. 重置信号源索引计数器 `_rand_src_idx = 0`
7. 设置初始 `_phase = "close"`
8. **启动前清理**：向所有选中的屏组发送**关闭所有窗口**请求（协议 80824），等待完成后再启动定时器和循环

---

## 六、循环逻辑（两阶段定时器驱动）

### 阶段 1 — close（关旧窗）

1. 从信号源迭代器取出下一个信号源 ID
2. 更新信号源索引显示（格式：`{信号源ID} ({序号}/{总数})`）
3. **为每个屏组 × 每个窗口槽位生成随机坐标**：
   - 窗口宽/高：在 100 ~ 上限值之间随机
   - 窗口位置：确保完全落在屏组范围内（x0 = random(0, total_w - rw)，y0 = random(0, total_h - rh)）
   - 宽/高上限自动取 `min(用户设定, 屏组实际宽/高)`
4. 将本轮的随机窗口列表和信号源保存到状态，供阶段 2 使用
5. 清空各屏组的旧窗口记录 `active_wins[screen_id] = []`
6. 向所有选中的屏组发送**关闭指定窗口**请求（协议 80828），关闭上一轮所有窗口
7. 设置 `_rand_worker_busy = True`
8. worker 的 `sig_finished` 信号触发 `_on_rand_close_done()`

### 阶段 1 → 阶段 2 过渡（`_on_rand_close_done`）

1. 清空 `_rand_worker`，重置 `_rand_worker_busy = False`
2. 如果 `_rand_running = False` 则直接返回
3. 将 `_phase` 设为 `"open"`
4. 启动**停留定时器**（`_rand_timer.start(dwell_ms)`）

### 阶段 2 — open（开新窗）

1. 将 `_phase` 重置为 `"close"`（为下一次循环做准备）
2. 取出阶段 1 保存的随机窗口列表和信号源
3. 向所有屏组发送**开窗**请求（协议 80826），使用随机生成的位置和尺寸
4. 设置 `_rand_worker_busy = True`
5. worker 的 `sig_done` 信号中逐个处理响应，记录每个屏组返回的新窗口 ID
6. worker 的 `sig_finished` 信号触发 `_on_rand_open_done()`

### 阶段 2 → 下一轮循环（`_on_rand_open_done`）

1. 重置 `_rand_worker_busy = False`
2. 如果 `_rand_running = False` 则直接返回
3. 启动**间隔定时器**（`_rand_timer.start(interval_ms)`），进入下一轮

### 防重入机制

- `_rand_worker_busy` 标志：worker 运行时定时器仍可能触发，此时跳过本次触发
- worker 的 `_stop` 标志：停止时设置，worker 内部检测到此标志后立即中断循环

---

## 七、停止流程

1. 设置 `_rand_running = False`，`_rand_worker_busy = False`
2. 停止定时器（`_rand_timer.stop()`）
3. 如果 worker 还在运行：设置 `_worker._stop = True`，等待其结束后清空
4. 恢复 UI 状态：启用"开始"和"步进开窗开始"按钮，禁用"停止"按钮，清空信号源显示
5. **异步关闭所有窗口**：遍历 `_rand_state["active_wins"]` 中每个屏组的所有窗口，向各屏组发送关闭请求（协议 80828）
6. 清空 `_rand_state`
7. 关闭完成后，通过 `QTimer.singleShot(0, ...)` 异步刷新窗口列表

---

## 八、UI 状态说明

| 状态 | 开始按钮 | 停止按钮 | 步进开始按钮 | 信号源显示 |
|------|---------|---------|------------|---------|
| 未运行 | 启用 | 禁用 | 启用 | — |
| 运行中 | 禁用 | 启用 | 禁用 | {ID} ({序号}/{总数}) |
| 停止中 | 启用 | 禁用 | 启用 | — |

---

## 九、与步进开窗的差异对照

| 特性 | 步进开窗 | 随机开窗 |
|------|---------|---------|
| 窗口位置 | 固定起始位置 | 每轮随机生成 |
| 窗口尺寸 | 逐步变化（步进） | 每轮随机生成（≤上限） |
| 每轮窗口数 | 所有选中屏组各 1 个 | 所有选中屏组各 N 个（可配） |
| 旧窗关闭 | 仅有关旧窗的屏组 | 所有选中屏组（`CloseAllWindows`） |
| 窗口 ID 记录 | `active_wins[screen_id] = win_id` | `active_wins[screen_id] = [win_id, ...]`（列表） |

---

## 十、协议号对照

| 功能 | 协议号 |
|------|--------|
| 关闭所有窗口 | 80824 |
| 关闭指定窗口 | 80828 |
| 开窗 | 80826 |

---

## 十一、模块互斥

- 开始前检查步进开窗是否运行：如有，先调用步进开窗的停止流程
- 运行期间，步进开窗的开始按钮被禁用
