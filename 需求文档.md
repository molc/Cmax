1.我的目录下面放着一个CronosPre项目控制卡通信协议.md和CronosPre项目控制卡通信协议_images。我需要你读懂相关的协议，进行程序编写时要对比文档进行。
2.我的需求是编写一个可编辑的软件，对设备的各项功能进行自由排序测试，先只编写一部分功能，后续要扩展，所以编写代码时，要注意后续的拓展。
3.目录还有一个登录信息.md，这是我现有的软件在进行登录时，需要从设备获取的信息。所以你登录时也需要获取同样的信息。
4.默认的IP地址设置为172.21.109.151，端口号2100，账号：amdin，密码：123456.先编写登录界面吧。需要将日志自动保存，并在UI界面显示，让我一目了然。
5.直接帮我生成UI界面
6.请查看日志，和登录信息.md进行对比，看看你的登录日志是否缺少东西。
7.


7.我现在要做一个拼接的小的功能：
（1）手动填写信号源（可填写多个），手动选择在已有的某个屏组上进行随机开窗操作。
（2）要读取当前设备有多少屏组，每个屏组的拼接规模，单口分辨率信息。窗口大小不要超过屏组的范围。举例：当前屏组ID为0的屏组，拼接规模是2*2，单口输出分辨率为1920*1080.所以窗口宽最大规模为3840，高度最高为2160.
（2）读取信号源的协议号为80400，读取拼接屏组的协议号为80800，读取矩阵输出逻辑映射的协议号为81200，读取KVM坐席的协议号为81600.注意区分各个模块的协议号的不同

8.点击查询信号源/查询屏组后，软件出现闪退。同时在登录的时候已经查询到了信号源和屏组，不需要再单独点击去查询这些信息。先帮我检查代码BUG

9.界面需要精简
（1）去除查询信号源，查询屏组，刷新窗口，关闭所有窗口按键。
（2）信号源/屏组自动查询，无需单独显示
（3）开窗操作模块需要将所有的屏组进行显示，方便勾选。
（4）开窗操作模块的信号源选择要手动填写，不能是固定值，需要一个范围值。开窗使用的信号源要依次对这个范围的值进行使用。
（5）开窗操作分为步进开窗和随机开窗2个模式
（6）步进开窗时，需要手动填写起始坐标x，y的值。结束位置x，y值按照步进值依次增加。若上一个结束位置的值加上步进值超过了屏组最大范围，就按照最大窗口显示。
（7）步进开窗需要支持循环操作。可正向步进，也可反向步进。
（8）随机开窗功能暂时不动，等待后续的需求。
（9）注意协议号的使用，不要错误的将拼接/矩阵/坐席混为一谈
（10）如果需求有不明确的，请提问，不要盲目去写。

10.你查看下日志，现在屏组一直显示等待加载中，同时日志存在报错。
[13:36:21] [INFO] 登录成功: UserID=1, Role=0, AToken=e9766f8e6f8fb98f...
[13:36:21] [DEBUG] 心跳定时器已启动（间隔 30s）
[13:36:21] [INFO] 自动加载信号源 (80400) 和屏组 (80800)...
[13:36:21] [ERROR] [80400] 请求失败: 'NoneType' object has no attribute 'encode'
[13:36:21] [ERROR] [80800] 请求失败: 'NoneType' object has no attribute 'encode'

11.需求还是存在一些偏差。
（1）步进开窗时，需要先将上一个窗口关闭再开新的窗口。
（2）每次开窗后的窗口停留时间手动填写
（3）窗口宽高最小规模为100*100
（4）起始位置手动填写后不要变化。
（5）多个屏组可以同时进行步进开窗，而不是现在的轮流使用屏组进行步进开窗。
（6）多个屏组进行开窗时，使用同一个信号源进行。
（7）信号源可以填写范围，也可以单独设置多个不同的信号源。多存在多个信号源时，每个窗口依次使用不同的信号源，循环使用。

12.没有步进效果

13.我要的步进效果如下
（1）窗口起始位置手动填写，不要变化。
（2）填写宽/高步进值后，要在原有的窗口大小基础上加上步进值，重新开窗。
（3）窗口初始大小可以手动填写

14.[20:04:47] [DEBUG] [4] 无旧窗
[20:04:48] [DEBUG] === 步进 cycle === src=31 pos=(0,0)-(100,100)
[20:04:48] [DEBUG] [4] 无旧窗
[20:04:48] [DEBUG] === 步进 cycle === src=31 pos=(0,0)-(100,100)
[20:04:48] [DEBUG] [4] 无旧窗
[20:04:49] [DEBUG] === 步进 cycle === src=31 pos=(0,0)-(100,100)
[20:04:49] [DEBUG] [4] 无旧窗
[20:04:49] [INFO] 步进已停止
[20:04:49] [DEBUG] 窗口列表刷新: 1 个窗口

15.出现错误：日志显示无旧窗

16.功能已经实现，现在有一个小的问题需要调整，宽步进、高步进的值支持填入0，也就是支持步进为0的效果

17.审查下代码，看看还有没有其他BUG。

18.继续添加新的功能：随机开窗
随机开窗需求如下：
（1）信号源的选择和步进开窗逻辑相同，不做修改。
（2）窗口的起始位置x/y值随机，窗口宽高随机。
（3）窗口的起始位置和结束位置要在当前屏组的范围内，不可超过最大值。举例：当前屏组ID为0的屏组，拼接规模是2*2，单口输出分辨率为1920*1080.所以窗口宽最大规模为3840，高度最高为2160.窗口不可超过这个范围
（4）窗口宽高最小规模为100*100

19.随机开窗的需求需要做如下调整
（1）每次随机开窗的数量要手动填写，支持超过1个窗口的操作。
（2）每个窗口的起始位置和大小均要随机。
（3）窗口宽/高的范围只需要填写最大值即可，最小值均默认为100.
（4）打开软件填写的数值需要保存，关闭软件再打开后，要能读取上一次关闭时填写的数值。

20出现了开窗异常，你读取日志，排查下问题

21.
（1）再屏组进行随机开窗时，需要先将该屏组所有窗口进行关闭，再进行随机开窗。
（2）我刚才多次随机开窗，发现所有窗口均在该屏组的第一个输出接口范围内，是不是代码编写错误？

22.现在增加新的功能-底图
（1）底图对应的协议号为80842-80858，其中我只需要使用80842，80848，80850这三条协议
（2）先查询当前设备保存的底图ID
（3）在勾选的拼接屏组上，轮流使能现有的底图ID。
（4）每次使能底图时，需要根据屏组的拼接规模，随机设置使能的端口。x/y/w/h最小按照单个接口的分辨率进行。举例：屏组ID0的拼接规模为2*2，单口分辨率为1920*1080。底图使能的位置可以为0，0，1920，1080。也可以为1920，1080，1920，1080，也可以为1920，0，1920，2160。x只能是0/1920,y只能是0/1080，w只能是1920/3840,h只能是1080/2160
(5)每次循环的停留时间需要手动填写。
（6）如果需求有不明确的，请提问，不要盲目去写。

23.未查询到底图数据

24.目录下有一个我用wireshark抓包的底图设置过程。文件名称叫设置底图.md，你对比下代码，是否有误。

25.功能正确，但软件点击后经常处于假死状态。点击停止可能无效。优化下代码性能。

26.做2处修改。
（1）登录的时候自动查询底图信息。不用再单独显示。
（2）勾选多个屏组时，让他们依次进行底图随机设置，不用一起进行。
（3）现在软件在勾选多个屏组时，很容易就卡死了。需要再优化下

27.日志提示80842请求超时。

28.代码运行正常。现在开发OSD滚动字幕功能
（1）滚动字幕的协议号为80860-80892.我只需要用几个功能即可，不需要使用全部的协议。
（2）设备登录后自动查询当前存在的屏组滚动字幕ID。
（3）在勾选的拼接屏上，轮流使能现有的字幕ID。
（4）每次使能字幕时，字幕的运动模式，方向，速度均要随机变化。
（5）目录下有一个屏组字幕.md。这是我用wireshark抓包后的正确数据。你要确保代码正确。
（6）如果需求有不明确的，请提问，不要盲目去写。

29.
（1）你通过对比屏组字幕.md判断我的需求需要使用的协议号
（2）每个字幕的ID均独立属于某个屏组，不是全局共享。
（3）如果性能可以的话，建议让所有的屏组一起进行操作，如果不可以，可以尝试让每个屏组间隔500ms.
（4）运动模式只有向上、向下、向左、向右4中模式。相关参数在协议号80866中写明
（5）速度的设置也在协议号80866中写明
（6）只切换已有的字幕ID，无需新建
（7）同一个屏组只能显示一个字幕。
（8）屏组字幕.md进行了更新，速度分别使用了快、较宽、慢三档。代码也只需要包含这3档即可

30.软件不闪退了，但没看到实际效果。我在屏组ID4进行了2次的字幕设置，通过wireshark抓包了数据，文件名称叫屏组字幕2.md.你对比下，修改代码。

31.我对屏组ID4进行了功能验证，发现并没有进行字幕的轮流设置效果。原有的字幕一直在运行



32.现在做新的需求
（1）目前有4个模块：步进开窗、随机开窗、底图、滚动字幕。我需要让其按照一定的顺序执行。顺序手动选择。
（2）每个模块需要增加运行时长。在顺序执行到该模块时，运行一定时长后，进行下一个模块的功能测试
（3）增加一个一键执行的按键。在手动选择各个模块的顺序后，一键执行。


