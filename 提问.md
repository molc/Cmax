# 步进开窗模块 — 功能需求文档

## 一、模块定位

步进开窗模块用于在选中的屏组上，以固定的起始位置和初始尺寸开窗，然后按照设定的步进量逐次增大（或减小）窗口尺寸，每次循环使用下一个信号源。当窗口超出屏组范围时自动停止。

---

## 二、前置条件

- 用户已成功登录设备
- 登录后自动加载信号源列表和屏组列表
- 至少选中一个屏组
- 已在信号源输入框中填写了有效的信号源范围（如 `0-3` 或 `0,1,2,3`）

---

## 三、参数说明

| 参数 | 控件类型 | 默认值 | 说明 |
|------|---------|--------|------|
| 起始 X | 数字输入框 | 0 | 窗口左上角 X 坐标 |
| 起始 Y | 数字输入框 | 0 | 窗口左上角 Y 坐标 |
| 窗口宽 | 数字输入框 | 100 | 初始窗口宽度，最小 100 |
| 窗口高 | 数字输入框 | 100 | 初始窗口高度，最小 100 |
| 宽步进 | 数字输入框 | 0 | 每次循环宽度增量，可为负数 |
| 高步进 | 数字输入框 | 0 | 每次循环高度增量，可为负数 |
| 停留时间 | 数字输入框 (ms) | — | 开窗后保持不变的时间 |
| 间隔时间 | 数字输入框 (ms) | — | 每次循环的总间隔（关窗+停留+开窗） |
| 信号源范围 | 文本框 | — | 支持 `0-3` 或 `0,1,2,3` 格式 |

---

## 四、验证规则（点击开始时执行）

1. **屏组检查**：未选中任何屏组 → 弹出警告框"请先选择至少一个屏组"
2. **信号源检查**：信号源输入为空或格式错误 → 弹出警告框"请填写正确的信号源（如 0-3 或 0,1,2,3）"
3. **窗口尺寸检查**：宽或高小于 100 → 弹出警告框"窗口宽高最小为 100x100"
4. **步进检查**：宽步进和高步进同时为 0 → 弹出警告框"宽步进和高步进不能同时为0（会无限循环）"
5. **范围检查**：起始位置 + 初始窗口 超出任一选中屏组的范围 → 弹出警告框"窗口超出屏组 X 范围"

范围默认值：宽 3840，高 2160（设备未返回范围时使用此默认值）。

---

## 五、开始流程

1. 检查是否有其他模块（随机开窗）在运行，如有则先停止
2. 执行上述验证规则，全部通过后继续
3. 禁用"开始"按钮和"随机开窗开始"按钮，启用"停止"按钮
4. 创建状态字典 `_step_state`，保存所有参数和当前状态
5. 用信号源列表创建无限迭代器 `itertools.cycle(src_ids)`
6. 重置信号源索引计数器 `_step_src_idx = 0`
7. 设置初始 `_phase = "close"`
8. 启动间隔定时器（`_step_timer.start(interval_ms)`）
9. 立即触发第一次循环 `_do_step_cycle()`

---

## 六、循环逻辑（两阶段定时器驱动）

### 核心概念：phase 标志

定时器每次触发时，根据 `_phase` 标志决定执行哪个阶段：

- `phase = "close"`：关旧窗阶段
- `phase = "open"`：开新窗阶段

### 阶段 1 — close（关旧窗）

1. 从信号源迭代器取出下一个信号源 ID
2. 更新信号源索引显示（格式：`{信号源ID} ({序号}/{总数})`）
3. 将本次要开窗的参数（信号源、位置、尺寸）保存到状态，供阶段 2 使用
4. 向所有选中的屏组发送**关闭指定窗口**请求（协议 80828），关闭上一轮开出的窗口（第一轮时无旧窗，直接跳过）
5. **防重入**：设置 `_step_worker_busy = True`，防止定时器在 worker 运行期间再次触发
6. worker 的 `sig_finished` 信号触发 `_on_step_close_done()`

### 阶段 1 → 阶段 2 过渡（`_on_step_close_done`）

1. 清空 `_step_worker`，重置 `_step_worker_busy = False`
2. 如果 `_step_running = False`（已停止）则直接返回
3. 将 `_phase` 设为 `"open"`
4. 启动**停留定时器**（`_step_timer.start(dwell_ms)`），等待停留时间到期后进入阶段 2

### 阶段 2 — open（开新窗）

1. 将 `_phase` 重置为 `"close"`（为下一次循环做准备）
2. 取出阶段 1 保存的开窗参数（信号源、位置）
3. 向所有选中的屏组发送**开窗**请求（协议 80826），参数包括：
   - 位置：(x0, y0) ~ (x1, y1)
   - 信号源：保存的信号源 ID
   - 边框宽度：0，无背景色
4. 设置 `_step_worker_busy = True`
5. worker 的 `sig_done` 信号中逐个处理响应，记录每个屏组返回的新窗口 ID（用于下次关闭）
6. worker 的 `sig_finished` 信号触发 `_on_step_open_done()`

### 阶段 2 → 下一轮循环（`_on_step_open_done`）

1. 清空 `_step_worker`，重置 `_step_worker_busy = False`
2. 如果 `_step_running = False` 则直接返回
3. **更新窗口尺寸**：`cur_w += step_w`，`cur_h += step_h`
4. 计算新右下角坐标：`new_x1 = x0 + cur_w`，`new_y1 = y0 + cur_h`
5. 更新 UI 显示当前窗口尺寸
6. **边界检查**：如果新坐标超出任一屏组范围，自动停止步进（打印日志，不弹框）
7. 启动**间隔定时器**（`_step_timer.start(interval_ms)`），进入下一轮循环

### 防重入机制

- `_step_worker_busy` 标志：worker 运行时定时器仍可能触发，此时跳过本次触发
- worker 的 `_stop` 标志：停止时设置，worker 内部检测到此标志后立即中断循环

---

## 七、停止流程

1. 设置 `_step_running = False`，`_step_worker_busy = False`
2. 停止间隔定时器（`_step_timer.stop()`）
3. 如果 worker 还在运行：设置 `_worker._stop = True`，等待其结束后通过 `finished` 信号清空
4. 恢复 UI 状态：启用"开始"按钮，禁用"停止"按钮，启用"随机开窗开始"按钮，清空信号源索引和窗口尺寸显示
5. **异步关闭所有窗口**：遍历 `_step_state["active_wins"]` 中的窗口，向各屏组发送关闭请求（协议 80828）
6. 清空 `_step_state`
7. 关闭完成后（`sig_finished` 信号），通过 `QTimer.singleShot(0, ...)` 异步刷新窗口列表

---

## 八、UI 状态说明

| 状态 | 开始按钮 | 停止按钮 | 随机开始按钮 | 信号源显示 | 窗口尺寸显示 |
|------|---------|---------|------------|-----------|------------|
| 未运行 | 启用 | 禁用 | 启用 | — | 当前窗口: — |
| 运行中 | 禁用 | 启用 | 禁用 | {ID} ({序号}/{总数}) | 当前窗口: {W}x{H} |
| 停止中 | 启用 | 禁用 | 启用 | — | 当前窗口: — |

---

## 九、边界条件处理

1. **第一轮无旧窗**：关窗阶段检测到 `active_wins` 中无记录时，跳过关窗，直接进入开窗
2. **宽/高步进为 0**：该方向不参与边界比较，窗口在该方向上保持初始尺寸
3. **宽/高步进为负数**：窗口逐步缩小，仍会触发边界停止（当坐标小于 0 时通过边界检查停止）
4. **负数坐标**：起始 X/Y 允许为 0，但不允许为负数（SpinBox 默认最小值为 0）
5. **worker 运行中停止**：设置 `_stop = True`，worker 内部检测后立即退出循环，不等待
6. **定时器触发时 worker 仍忙**：跳过本次触发，等待下一次定时器到期

---

## 十、协议号对照

| 功能 | 协议号 |
|------|--------|
| 关闭指定窗口 | 80828 |
| 开窗 | 80826 |

---

## 十一、模块互斥

- 开始前检查随机开窗是否运行：如有，先调用随机开窗的停止流程
- 运行期间，随机开窗的开始按钮被禁用（防止误操作）
