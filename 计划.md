# CronosPre 设备测试软件 - 实现计划

## Context

用户需要一个可编辑的设备测试软件，对 CronosPre 控制卡进行自由排序测试。协议基于 TCP/XML，使用 HTTP Digest 认证。
- **语言**: Python + PyQt5
- **第一期**: 连接 + 心跳 + 设备信息查询
- **核心交互**: 拖拽排序命令队列
- **设备IP**: 172.21.109.151 (端口 **2100**)
- **测试账号**: admin
- **真实会话数据**: 见 `登录信息.md`，已验证流程正确

---

## 设备真实信息（来自登录信息.md）

| 字段 | 值 |
|---|---|
| 设备型号 | CronosPre (ModelNumber: 2400) |
| 机器规模 | 12U (machineU: 12) |
| 最大卡槽 | 40 (MaxVSlotNum: 40) |
| 最大输入通道 | 160 (MaxVIn: 160) |
| 最大输出通道 | 160 (MaxVOut: 160) |
| 控制卡版本 | 0.8.6.14.cm (CenterCtrl) |
| 控制卡日期 | 2026-04-10 17:44:27 |
| 预监服务端口 | 3000 |
| License超时 | Timeout=0 (永久), Bootout=0 (永久) |
| 用户ID | 1 |
| AToken | 18fefb95afd5409081aaf6313591389d |
| Role | 0 (admin) |
| 登录nonce | 08c2041637d10d2f8afae61a4af9a53f |
| digest response | ec8bccab82acb1e4d78254311969adb8 |

---

## 项目结构

```
cronos_pre_tester/
├── main.py                          # 程序入口
├── config.py                       # 常量定义
├── protocol/
│   ├── header.py                  # 24字节二进制协议头
│   ├── message.py                 # 消息打包/解包
│   ├── socket_wrapper.py          # TCP 套接字封装
│   ├── digest_auth.py             # 摘要认证
│   └── session.py                 # 会话状态 (UserID, AToken, serial)
├── plugins/
│   ├── __init__.py               # 插件注册表 (PLUGIN_REGISTRY)
│   ├── base.py                   # ProtocolPlugin 基类
│   ├── login_plugin.py           # 登录 (80000/80001, 两步认证)
│   ├── heartbeat_plugin.py       # 心跳 (80004/80005)
│   ├── hardware_info_plugin.py   # 硬件信息 (80026/80027)
│   ├── power_query_plugin.py     # 电源查询 (80118/80119)
│   ├── fan_query_plugin.py       # 风扇查询 (80112/80113)
│   ├── board_temp_plugin.py      # 板卡温度 (80028/80029)
│   ├── system_time_plugin.py      # 系统时间 (80070/80071)
│   └── license_timeout_plugin.py  # License超时 (80022/80023)
├── queue/
│   ├── command_item.py           # 单个排队命令 (QueuedCommand)
│   └── command_queue.py          # QAbstractTableModel + 拖拽排序
├── ui/
│   ├── main_window.py            # 主窗口 (QSplitter 布局)
│   ├── connection_bar.py         # 连接栏 (IP/端口/账号/密码/连接按钮)
│   ├── command_panel.py          # 命令面板 (QTreeWidget 分类列表, 可拖拽)
│   ├── queue_panel.py            # 队列面板 (QTableView 拖拽排序 + 执行按钮)
│   ├── response_viewer.py        # 响应查看器 (XML + 提取字段表格)
│   └── status_log_panel.py       # 状态日志面板
└── utils/
    └── xml_utils.py              # XML 格式化 / XPath 提取
```

---

## 1. 协议层 (protocol/)

### 1.1 24字节协议头 (header.py)

```
Offset  Size  Field          Value
0       2     marker         0xFFFF
2       2     version        0x0001
4       4     protocol_num   请求号(偶数) / 响应号(奇数)
8       4     serial         递增序号
12      4     status_code    请求=0, 响应=200/4xx/5xx
16      4     body_length    协议体字节数
20      4     body_type      0=XML, 1=二进制
```

打包格式: `!HHIIIII` (大端序, struct 模块)

### 1.2 消息封装 (message.py)

- `ProtocolMessage.to_bytes()`: header.pack() + xml_body.encode('utf-8')
- `ProtocolMessage.from_socket()`: 先收24字节头, 再收body_length字节体
- `_recv_exact()`: 循环recv确保收满指定字节数, 返回None表示断开

### 1.3 摘要认证 (digest_auth.py)

```
response = md5(md5(user:realm:pwd) : nonce : md5(method:uri))

淳中: realm="Athena2", method="Login"(中文)/"LoginEN"(英文)
中性: realm="Athena-ii", method="Register"/"RegisterEn"
```

### 1.4 会话状态 (session.py)

```python
@dataclass
class Session:
    user_id:     Optional[int] = None
    a_token:     Optional[str] = None
    user_id_str: str = ""      # XML模板中使用，格式: <UserID atoken="...">ID</UserID>
    serial:      int = 0      # 原子递增

    def next_serial(self) -> int:
        self.serial += 1
        return self.serial

    def user_id_xml(self) -> str:
        """生成带atoken属性的UserID标签"""
        if self.a_token:
            return f'<UserID atoken="{self.a_token}">{self.user_id}</UserID>'
        return f'<UserID>{self.user_id}</UserID>'
```

---

## 2. 插件系统 (plugins/)

### 2.1 基类设计 (base.py)

```python
class FieldExtractor:
    key:  str                    # 显示标签
    xpath: str                   # XPath 表达式
    fmt: Optional[Callable] = None  # 格式化函数

class ProtocolPlugin(ABC):
    @property @abstractmethod
    def name(self) -> str: ...

    @property @abstractmethod
    def category(self) -> str: ...

    @property @abstractmethod
    def request_proto(self) -> int: ...  # 偶数

    @property
    def response_proto(self) -> int:
        return self.request_proto + 1     # 自动推导奇数响应号

    @property @abstractmethod
    def request_xml_template(self) -> str: ...  # Python format string

    @property @abstractmethod
    def response_xpaths(self) -> list[FieldExtractor]: ...

    def build_request_xml(self, session, **user_inputs) -> str:
        # 默认: 用 session.user_id_str 填充模板
        return self.request_xml_template.format(user_id=session.user_id_str)

    def parse_response(self, xml_body: str) -> dict:
        # 默认: ElementTree + XPath 提取
        result = {}
        for fe in self.response_xpaths:
            node = root.find(fe.xpath)
            val = node.text if node is not None else "N/A"
            result[fe.key] = fe.fmt(val) if fe.fmt else val
        return result
```

### 2.2 插件注册 (__init__.py)

```python
PLUGIN_REGISTRY: dict[int, type[ProtocolPlugin]] = {}

def register_plugin(cls: type[ProtocolPlugin]) -> type[ProtocolPlugin]:
    PLUGIN_REGISTRY[cls().request_proto] = cls
    return cls

# 使用方式:
# @register_plugin
# class HardwareInfoPlugin(ProtocolPlugin): ...
```

### 2.3 登录插件 (login_plugin.py) - 特殊处理

两步流程由 SessionManager 直接处理，不走通用执行器：

```
1. 发 80000 无认证请求
2. 收到 401, 从XML提取 nonce/realm/uri
3. 计算 digest = md5(md5(user:realm:pwd):nonce:md5(Login:uri))
4. 发 80000 带 <Authorization method="Digest"> 的请求
5. 收到 200, 提取 UserID/AToken/Role
```

SessionManager.login() 内部完成此流程, 成功后设置 session 并启动心跳。

### 2.4 心跳插件 (heartbeat_plugin.py)

```python
# 真实请求格式（带atoken属性）：
request_xml_template = (
    '<?xml version="1.0" encoding="utf-8"?>'
    '<Message>{user_id_with_token}</Message>'
)
# 其中 {user_id_with_token} 由 session.user_id_xml() 生成:
# <UserID atoken="18fefb95afd5409081aaf6313591389d">1</UserID>

response_xpaths = [FieldExtractor("Server Time", ".//Time")]
```

---

## 3. 拖拽排序队列 (queue/)

### 3.1 QueuedCommand 数据类

```python
@dataclass
class QueuedCommand:
    id:           str                    # uuid, 队列中唯一
    plugin:       ProtocolPlugin         # 插件引用
    user_inputs:  dict[str, Any] = field(default_factory=dict)
    # 运行时填充:
    status:       str = "pending"       # pending/running/success/error
    request_xml:  str = ""
    response_xml: str = ""
    parsed:       dict = field(default_factory=dict)
    error:        str = ""
    duration_ms:  float = 0.0
```

### 3.2 CommandQueueModel (QAbstractTableModel)

| 列索引 | 列名 | 说明 |
|---|---|---|
| 0 | # | 行号 |
| 1 | 命令 | 插件显示名 |
| 2 | 状态 | pending/running/success/error |
| 3 | 耗时(ms) | 执行耗时 |
| 4 | 关键结果 | 前3个解析字段 或 错误信息摘要 |

关键实现:

```python
def flags(self, index):
    base = Qt.ItemIsSelectable | Qt.ItemIsEnabled
    if index.isValid():
        return base | Qt.ItemIsDragEnabled
    return base | Qt.ItemIsDropEnabled | Qt.ItemIsDragEnabled

def dropMimeData(self, data, action, row, column, parent):
    src_row = int(data.data("application/x-cronos-command-index").data())
    dest_row = parent.row() if parent.isValid() else len(self._items)
    if src_row < dest_row:
        dest_row -= 1
    # 移动 item 到 dest_row 位置
```

---

## 4. 执行器 (queue/command_executor.py)

```python
class CommandExecutor(QThread):
    sig_started   = pyqtSignal()
    sig_item_done = pyqtSignal(int, bool)   # row, success
    sig_all_done  = pyqtSignal()

    def run(self):
        for row, cmd in enumerate(self._queue._items):
            if self._stop_requested:
                break
            self._execute_item(row, cmd)
        self.sig_all_done.emit()

    def _execute_item(self, row, cmd):
        # 1. 构建协议头和消息
        header = ProtocolHeader(
            protocol_num = cmd.plugin.request_proto,
            serial       = self._session.next_serial(),
        )
        xml = cmd.plugin.build_request_xml(self._session, **cmd.user_inputs)
        cmd.request_xml = xml

        # 2. 发送
        self._socket.send_message(ProtocolMessage(header, xml))

        # 3. 接收
        resp, err = self._socket.recv_message()
        cmd.duration_ms = elapsed

        # 4. 解析响应
        if err:
            cmd.status = "error"; cmd.error = err
        elif resp.header.protocol_num != cmd.plugin.response_proto:
            cmd.status = "error"; cmd.error = f"响应号不匹配"
        else:
            cmd.parsed = cmd.plugin.parse_response(resp.xml_body)
            cmd.status = "success" if resp.header.status_code == 200 else f"error_{resp.header.status_code}"

        self.sig_item_done.emit(row, cmd.status == "success")
```

---

## 5. 心跳机制

- SessionManager 持有 QTimer (30秒间隔)
- 登录成功后启动, 断开时停止
- 独立于命令队列运行, 但共享同一个 socket
- 心跳超时认为连接断开

---

## 6. UI 布局 (main_window.py)

```
+------------------------------------------------------------------+
| 连接栏: IP [________] 端口 [____] 用户 [____] 密码 [____] [连接]   |
+------------------------------------------------------------------+
| 命令面板 |              队列面板                   |  响应查看器    |
|  (220px) |  [Run Queue] [Remove] [Clear]         |   (350px)     |
| [Session]| +---+----------------------+----------+ |              |
|  Login   | | 1 | Hardware Info       | pending | | XML响应:      |
|  Heartbeat| | 2 | Power Query        | success | | <Response>   |
| [System] | | 3 | Fan Query           | error   | |   ...        |
|  Time    | +---+----------------------+----------+ |              |
| [Hardware]|                                      | 提取字段:     |
|  Hardware |                                      | MaxVIn: 40   |
|  Power    |                                      | MaxVOut: 40  |
|  Fan      |                                      |              |
|  Board Temp|                                     |              |
| [Status] |                                      |              |
|  License  |                                      |              |
+------------+-------------------------------------+--------------+
| 日志面板: [INFO] Connected to 172.21.109.151:2100                |
|          [INFO] Login successful. UserID=1                      |
|          [INFO] [2] Power Query: success (320.5ms)             |
+------------------------------------------------------------------+
```

- 命令面板: QTreeWidget, 按类别分组, 拖拽子项到队列
- 队列面板: QTableView + 自定义拖拽, 点击行选中查看响应
- 响应查看器: XML格式化显示 + 字段提取表格
- 日志面板: 滚动日志, 按 INFO/ERROR/WARN 着色

---

## 7. 第一期命令清单

| 命令 | 请求号 | 响应号 | 关键提取字段 | 真实请求示例 |
|---|---|---|---|---|
| 登录 | 80000 | 80001 | UserID, AToken, Role | 两步: 无Auth -> 401(nonce) -> 带Digest -> 200 |
| 心跳 | 80004 | 80005 | Time | `<UserID atoken="...">1</UserID>` |
| 硬件信息 | 80026 | 80027 | machineU, MaxVSlotNum, MaxVIn, MaxVOut, ModelNumber, IOBoard/board | `<UserID atoken="...">1</UserID>` |
| 板卡温度 | 80028 | 80029 | Board[@slot,@temp] | `<UserID atoken="...">1</UserID>` |
| License超时 | 80022 | 80023 | Timeout(秒), Bootout(次) | `<UserID atoken="...">1</UserID>` (实测: Timeout=0, Bootout=0) |
| 风扇/机箱温度 | 80112 | 80113 | Fan[@value,@gear], Rpm[10个], Crate[@temp] | `<UserID atoken="...">1</UserID>` |
| 电源查询 | 80118 | 80119 | Slot[@index,@voltage,@current,@power,@temp] | `<UserID atoken="...">1</UserID>` (实测: 2个Slot, 约236V, 1.2A, 300W) |
| 系统时间 | 80070 | 80071 | DateTime (YYYY-mm-dd HH:MM:SS) | `<UserID atoken="...">1</UserID>` |
| 设备ID查询 | 80018 | 80019 | DevId | `<UserID atoken="...">1</UserID>` |
| 用户查询 | 80010 | 80011 | UserList/User | `<UserID atoken="...">1</UserID>` |
| 分组查询 | 80066 | 80067 | Groups/Group | `<UserID atoken="...">1</UserID>` |
| 输入信号源查询 | 80400 | 80401 | PhyPort/LogicalPort | `<UserID atoken="...">1</UserID>` (实测: 160通道, 有效通道24,28,32,34,36,38) |
| 输入分辨率查询 | 80434 | 80435 | Port[@id,@valid,@w,@h,@rate] | `<UserID atoken="...">1</UserID>` (ptype="0", type="all") |

---

## 8. 后续扩展点

| 扩展方向 | 操作方式 |
|---|---|
| 新增命令 | 在 `plugins/` 新建 `xxx_plugin.py`, 继承 ProtocolPlugin, 加 @register_plugin 装饰器即可 |
| 新增类别 | 在 `PluginCategory` 添加常量, 命令面板自动按类别分组 |
| 命令带参数 | QueuedCommand.user_inputs 存储参数, plugin.build_request_xml 使用参数 |
| 二期矩阵切换 | 新增 plugins/matrix_plugin.py, 实现 81200+ 系列协议 |
| 二期拼接操作 | 新增 plugins/wall_plugin.py, 实现 80800+ 系列协议 |
| 二期KVM坐席 | 新增 plugins/kvm_plugin.py, 实现 81600+ 系列协议 |

---

## 9. 依赖

```txt
PyQt5>=5.15
```

安装: `pip install PyQt5`

---

## 10. 真实会话数据验证（来自登录信息.md）

### Digest Auth 验证

已知数据:
- 用户名: `admin`
- realm: `Athena2`
- nonce: `08c2041637d10d2f8afae61a4af9a53f`
- uri: `172.21.109.151:2100`
- method: `Login`
- 预期 response: `ec8bccab82acb1e4d78254311969adb8`

验证公式:
```
HA1 = md5("admin:Athena2:password")
HA2 = md5("Login:172.21.109.151:2100")
response = md5(f"{HA1}:{nonce}:{HA2}")
```
用密码代入验证即可确认计算正确。

### 心跳/授权请求格式

所有登录后的请求使用带 `atoken` 属性的 UserID 标签:
```xml
<UserID atoken="18fefb95afd5409081aaf6313591389d">1</UserID>
```

### 电源查询响应示例（实测）
```xml
<Power runtime="6564">
  <Slot index="1" enable="1" voltage="236.25" current="1.15" temperature="35.50" power="272.24"/>
  <Slot index="0" enable="1" voltage="235.50" current="1.27" temperature="34.75" power="298.05"/>
</Power>
```

### 输入分辨率查询请求格式（实测）
```xml
<?xml version="1.0" encoding="utf-8"?>
<Message>
  <UserID atoken="18fefb95afd5409081aaf6313591389d">1</UserID>
  <PortList ptype="0" type="all"/>
</Message>
```

---

## 11. 验证计划

1. **协议层**: 用 `登录信息.md` 中的真实会话数据验证协议头解析和XML编解码
2. **Digest认证**: 用文件中的 nonce/response 验证摘要认证计算正确性
3. **登录流程**: 连接 172.21.109.151:2100，使用 admin 账号完成两步登录
4. **UI测试**: 连接后拖入多个命令，调整顺序后执行，验证顺序正确、响应正确解析
5. **拖拽测试**: 上下拖动队列项，验证序号自动更新
6. **错误处理**: 模拟断连，验证队列执行中断、日志提示
