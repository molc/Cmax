# 底图轮询模块 — 功能需求文档

## 一、模块定位

底图轮询模块用于对选中的屏组循环设置不同的底图。设备支持多张底图，每张底图有 ID、名称、宽、高。模块按顺序依次将每张底图设置到每个屏组上，每个屏组独立维护自己的底图索引，所有屏组都轮完一轮后再重新开始。

---

## 二、前置条件

- 用户已成功登录设备
- 登录后自动加载屏组列表
- 至少选中一个屏组
- 底图列表已通过「查询底图列表」按钮加载（在底图 Tab 左侧区域）

---

## 三、参数说明

| 参数 | 控件类型 | 默认值 | 说明 |
|------|---------|--------|------|
| 停留时间 | 数字输入框 (ms) | — | 每个底图显示的时间，最小 100ms |
| 间隔时间 | 数字输入框 (ms) | — | 相邻两次设置之间的间隔（由 dwell_ms 决定，无独立间隔参数） |

**注**：底图模块不依赖信号源输入框，无信号源相关参数。

---

## 四、前置操作：查询底图列表

1. 用户点击「查询底图列表」按钮
2. 向所有选中的屏组发送**查询底图列表**请求（协议 81204）
3. 解析响应，填充 `self._backdrops` 列表，每项包含 `id`、`name`、`w`、`h`
4. 将底图列表展示在左侧表格中（4 列：ID、名称、宽、高）
5. 状态标签更新：
   - 有底图：`共 N 个底图，可开始轮询`（绿色）
   - 无底图：`未查询到底图，可开始轮询`（红色）

---

## 五、验证规则（点击开始时执行）

1. **屏组检查**：未选中任何屏组 → 弹出警告框"请先选择至少一个屏组"
2. **底图列表检查**：`_backdrops` 为空 → 弹出警告框"底图列表尚未加载，请稍候"
3. **停留时间检查**：停留时间小于 100ms → 弹出警告框"停留时间最小为 100ms"

---

## 六、开始流程

1. 检查步进开窗、随机开窗是否运行，如有则先停止
2. 执行上述验证规则
3. 禁用"开始"按钮和"步进/随机开窗开始"按钮，启用"停止"按钮
4. 为每个屏组创建独立的底图索引 `bk_indices[screen_id] = 0`
5. 创建状态字典 `_bk_state`，包含 groups、backdrops、bk_indices、dwell_ms、_screen_idx（当前处理到第几个屏组）
6. 立即触发第一次轮询 `_do_bk_cycle()`

---

## 七、循环逻辑

底图轮询不是定时器驱动的两阶段模式，而是**逐个屏组串行处理**，每完成一个屏组后启动定时器等待 dwell，再处理下一个。

### 主体流程（`_do_bk_cycle`）

1. 如果 `_bk_running = False` 或 `_bk_worker_busy = True` 则返回
2. 根据 `_screen_idx` 取当前要处理的屏组 `gid`
3. 根据该屏组的底图索引 `bk_indices[gid]` 取当前底图 `bk`
4. **随机生成底图位置**（基于屏组的拼接格局）：
   - 从屏组信息获取 `hnum`（横屏数）和 `vnum`（纵屏数）
   - `col = random(0, hnum-1)`，`row = random(0, vnum-1)`
   - `cols = random(1, hnum-col)`，`vrows = random(1, vnum-row)`
   - 即随机选择一个起始物理屏，然后随机跨越若干个物理屏
5. 推进该屏组的底图索引：`bk_indices[gid] = (bk_idx + 1) % len(backdrops)`
6. 更新 UI 标签显示当前进度
7. 启动 **BkCycleWorker**（专用 worker，支持多个 screen_tasks）发送**设置底图**请求（协议 80850）
8. worker 的 `sig_done` 信号触发 `_on_bk_one_done()`

### 单个屏组完成后（`_on_bk_one_done`）

1. 清空 `_bk_worker`，重置 `_bk_worker_busy = False`
2. 如果 `_bk_running = False` 则直接返回
3. 推进屏组指针：`state["_screen_idx"] = (idx + 1) % len(groups)`
4. **刚轮完一圈的判断**（`_screen_idx` 从最后一个屏组绕回 0）：
   - 重置所有屏组的底图索引为 0
   - 更新标签显示
5. 启动**停留定时器**（`_bk_timer.start(dwell_ms)`）

**与步进/随机开窗的本质区别**：底图轮询的定时器不是在循环开始时统一启动，而是在**每个屏组设置完成后**启动，等待 dwell 后再处理下一个。

### 防重入机制

- `_bk_worker_busy` 标志：worker 运行时定时器触发则跳过

---

## 八、停止流程

1. 设置 `_bk_running = False`，`_bk_worker_busy = False`
2. 停止定时器（`_bk_timer.stop()`）
3. 如果 worker 还在运行：设置 `_worker._stop = True`，等待其结束后清空
4. 恢复 UI 状态：启用"开始"和"步进/随机开窗开始"按钮，禁用"停止"按钮，清空底图索引显示
5. 清空 `_bk_state`

**注**：停止时不执行异步关窗操作（底图不是窗口，无需关闭）。

---

## 九、UI 状态说明

| 状态 | 开始按钮 | 停止按钮 | 步进/随机开始 | 底图索引显示 |
|------|---------|---------|-------------|------------|
| 未运行 | 启用 | 禁用 | 启用 | — |
| 运行中 | 禁用 | 启用 | 禁用 | `{底图ID} ({序号}/{总数})` |
| 停止中 | 启用 | 禁用 | 启用 | — |

---

## 十、多屏组独立索引示意

假设有底图 [A, B, C]，屏组 [G1, G2]：

```
G1 设置 A → G1 索引: B
G2 设置 A → G2 索引: B
G1 设置 B → G1 索引: C
G2 设置 B → G2 索引: C
G1 设置 C → G1 索引: A（一轮完成）
G2 设置 C → G2 索引: A（一轮完成）
→ 重置所有索引为 0，重复以上
```

---

## 十一、协议号对照

| 功能 | 协议号 |
|------|--------|
| 查询底图列表 | 81204 |
| 设置底图 | 80850 |

---

## 十二、模块互斥

- 开始前检查步进开窗、随机开窗是否运行：如有，先停止
- 运行期间，步进/随机开窗的开始按钮被禁用
